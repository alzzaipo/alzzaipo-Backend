# default
spring:
  profiles:
    active: test

---
spring:
  config:
    activate:
      on-profile: test
    import: application-config.yml

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;
    username: sa
    password: