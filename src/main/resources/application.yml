# default
spring:
  profiles:
    active: prod

---
spring:
  config:
    activate:
      on-profile: local
    import: application-config.yml

---
spring:
  config:
    activate:
      on-profile: prod
    import: application-config.yml
---
spring:
  config:
    activate:
      on-profile: test
    import: application-config.yml